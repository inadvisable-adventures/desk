**/__pycache__/
**/*.pyc
**/.venv/

**/build/
**/dist/
**/node_modules/

**/*.egg-info/

# MacOS
**/.DS_Store

# Vim
**/*.swp
**/*.swo

# Desk state files (see design-docs/architecture.md's Desk Model) —
# generated per-directory session state, not project source.
**/*.desk

.desk_temp/

# Event mediator log (desk.event_mediator, TODO 6f9c51b) — generated
# per-directory runtime traffic, not project source, same as the Desk
# state files above.
MEDIATED-EVENT-LOG.tsv

# Whisper models (see design-docs/whisper-model-setup.md, TODO
# f9d2dc7) are never meant to live in this repo -- they're cached
# under the Hugging Face Hub cache instead. This is a defensive
# backstop only, in case a manual download ever lands here by mistake.
/whisper-*/
